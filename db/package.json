{
  "name": "@retail-thin-clone/db-tools",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "migrate": "node ./migrate.mjs",
    "seed": "node ./seed/seed-oracle.mjs",
    "generate:sample": "node ./seed/write-sample-json.mjs"
  },
  "dependencies": {
    "oracledb": "^6.7.0"
  }
}
